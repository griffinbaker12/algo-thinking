#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// has to be different by ONE character, and then the check becomes if the
// character is different after a chain of correct characters, and checking if
// adjacent characters are the same
int main() {}
